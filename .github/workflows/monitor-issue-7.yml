# Generated by Copilot
name: Monitor Issue #7 Comments

on:
  schedule:
    - cron: '*/15 * * * *'   # Every 15 minutes
  workflow_dispatch:

permissions:
  issues: write
  contents: write

jobs:
  monitor-issue:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install requests

      - name: Run Issue #7 monitor
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: Stan2891/bmparts-ai
          ISSUE_NUMBER: "7"
        run: |
          python .github/issue-monitor/monitor.py

      - name: Commit updated state
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .github/issue-monitor/state.json
          git commit -m "Update issue #7 monitor state" || echo "No changes to commit"
          git push
